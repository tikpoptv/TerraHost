# Core geospatial libraries (install via pip)
rasterio>=1.3.9
geopandas>=0.14.0
shapely>=2.0.0
fiona>=1.9.0
pyproj>=3.6.0

# Data processing
pandas>=2.0.0
scipy>=1.11.0

# Utilities
python-dotenv>=1.0.0

# Note: GDAL, numpy, pillow, requests are installed via apk
