# TerraHost Backend Environment Variables

# Server Configuration
PORT=8000
NODE_ENV=development

# Database (เตรียมไว้สำหรับอนาคต)
# DATABASE_URL=postgresql://user:password@localhost:5432/terrahost

# CORS (เตรียมไว้สำหรับอนาคต)
# FRONTEND_URL=http://localhost:3000
