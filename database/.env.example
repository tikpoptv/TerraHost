# TerraHost Database Configuration
# Copy this file to .env and modify as needed

# Database Connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=terrahost
DB_USER=postgres
DB_PASSWORD=password

# Optional: PostgreSQL Admin Connection (for database creation)
# POSTGRES_ADMIN_USER=postgres
# POSTGRES_ADMIN_PASSWORD=password
